=== 1. 模型容量 vs 训练误差 / 验证误差（30 个点拟合 sin）===
  手写 lstsq 与 sklearn 的 3 次多项式预测最大差 1.3e-14
    次数     训练 MSE     验证 MSE   判断
     1      0.272      0.308   欠拟合（两个都高）
     3      0.082      0.094   合适
     5      0.056      0.114   合适
     9      0.051      0.105   合适
    15      0.049      0.138   过拟合（训练低、验证高）
    25      0.040      0.313   过拟合（训练低、验证高）
  噪声方差 0.09 是验证 MSE 的下限：再好的模型也降不到它以下（不可约误差）
  图已保存 out/01-fit-three-degrees.svg
  图已保存 out/01-error-vs-degree.svg

=== 1b. 学习曲线：同一个模型，数据越多泛化越好；高容量模型需要更多数据 ===
  次数  4: n=15: 训练 0.057 / 验证 0.156  n=30: 训练 0.073 / 验证 0.111  n=100: 训练 0.096 / 验证 0.102  n=1000: 训练 0.092 / 验证 0.097
  次数 15: n=15: 训练 0.026 / 验证 20.870  n=30: 训练 0.053 / 验证 0.169  n=100: 训练 0.088 / 验证 0.102  n=1000: 训练 0.088 / 验证 0.093
  图已保存 out/01-learning-curve.svg
  数据一多，15 次多项式也不过拟合了：过拟合是「容量相对于数据太大」，不是容量本身的罪

=== 2. 训练 / 验证 / 测试三份各干什么 ===
  训练 180 / 验证 60 / 测试 60
  用验证集选出次数 6（验证 MSE 0.085）；测试集只在最后看一次：测试 MSE 0.084

=== 2b. 随机划分 vs 按组划分：同一来源的近重复样本必须在同一侧 ===
    次数    随机 5 折 CV    按组 5 折 CV       全新数据
     3        0.060        0.066      0.101
     9        0.061        0.088      0.098
    15        0.060        0.088      0.102
  随机切：每个验证样本的 4 个「兄弟」都在训练集里，高次多项式背下它们，CV 分数虚高；按组切的分数才接近全新数据

=== 2c. 污染检测：测试题的 n-gram 在训练语料里查 ===
  原题（GSM8K 风格，被抓进语料）       8-gram 重叠率 0.77
  改写（数字换了）                 8-gram 重叠率 0.05
  无关题                      8-gram 重叠率 0.00
  重叠率接近 1 → 原题在语料里；换了数字的改写重叠率骤降——n-gram 检测抓得住原文、抓不住改写（GPT-4 报告用 50 字符子串，Llama 3 用 8-gram 的 token 重叠）

=== 3. 测试集泄漏：用测试集选超参数，报出来的数字偏乐观 ===
  200 次重复：'在测试集上选出的最好分数' 比 '同一模型在全新数据上的真实分数' 平均乐观 0.255（MSE），68% 的情况下真实更差；中位数 0.015
  图已保存 out/01-test-set-selection-bias.svg
  这就是 benchmark 上反复调 prompt / 选 checkpoint 之后，分数不再可信的原因

=== 4. 偏差-方差分解与集成（bagging）===
  次数  1: 偏差² 0.158  方差 0.025   → 20 个模型平均后方差 0.001（偏差² 0.152 基本不变）
  次数  4: 偏差² 0.003  方差 0.014   → 20 个模型平均后方差 0.001（偏差² 0.003 基本不变）
  次数 15: 偏差² 0.008  方差 0.662   → 20 个模型平均后方差 0.341（偏差² 0.001 基本不变）
  图已保存 out/01-bias-variance-bands.svg
  低次：偏差大（模型太简单，系统性地错）；高次：方差大（对哪 30 个点太敏感）；平均多个模型只降方差
  LLM 里的对应：self-consistency 多次采样投票、多个 judge 取平均、model soup —— 都是在降方差

