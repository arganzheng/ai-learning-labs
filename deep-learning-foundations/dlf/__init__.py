"""dlf — the tiny NumPy framework built up across the 深度学习基础 series.

nn      Linear / ReLU / softmax-CE / MLP, gradient check, FLOP counter   (post 01)
layers  LayerNorm / RMSNorm / Residual / Dropout / Embedding, deep MLPs   (posts 02, 04)
optim   SGD / Momentum / Adam / AdamW, warmup + cosine, clipping          (post 03)
data    MNIST download + loading, a plain-text corpus for the char LM
cli     shared --quick / experiment-selection flags
"""
