=== 1. 三类 connector：同一份 4×4=16 个 patch 特征（d_v = 8）进去，出来多少个 token、丢不丢信息 ===
  MLP projector：(16, 8) → (16, 12)；参数 264（d_v·d + d² 量级）
  2×2 merge：(16, 8) → 拼接 (4, 32) → MLP (4, 12)；token 数 16 → 4，拼接不丢任何数（32 个数原样在）
  pixel (un)shuffle 得到的矩阵与手工 2×2 拼接是否只差通道顺序：True
  2×2 平均池化：(16, 8) → (4, 8)；四个 patch 之间的差别被抹掉，抹掉的部分（均方）0.84，占原方差的 77%
  resampler（K = 3 个 query）：(16, 8) → (3, 12)；不论图有多少 patch，永远输出 3 个 token
     第 0 个 query 的注意力分布（16 个 patch）：[0.07 0.06 0.08 0.07 0.07 0.04 0.06 0.05 0.02 0.05 0.09 0.07 0.07 0.08
 0.07 0.05]
  图已保存 out/02-connectors.svg

=== 2. 一张图占多少 token：三种分辨率策略 ===
  图                        固定 336²  AnyRes ≤4 tile      原生动态(28px)
  手机截图 1080×2400               576            2304            1280
  A4 文档照片 3000×2100            576            2880            1280
  小图标 64×64                    576            1152             256
  横幅 400×1600                  576            2304             798
  普通照片 768×1024                576            2880             999
  原生动态 = 像素数 / 784：64×64 只要 5 个 token（被下限 256 顶住），一页文档 3000×2100 = 8036 → 被上限 1280 截住
  图已保存 out/02-token-budget.svg

