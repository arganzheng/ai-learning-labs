# CPU-only is enough for every experiment in this repo.
# Install the CPU wheel of PyTorch first if you do not want the CUDA build:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
numpy>=2.0
torch>=2.4
torchvision>=0.19
# algorithm-tooling/ and classical-ml/ (算法地图 L1 / L2):
pandas>=2.0
matplotlib>=3.8
scikit-learn>=1.4
# Only transformer-and-llm/tokenizer_compare.py needs these two (and network access):
tiktoken>=0.7
tokenizers>=0.19
# post-training/ (trl trainers on Qwen2.5-0.5B / 1.5B; MPS or CUDA recommended):
transformers>=4.50
trl>=0.20
peft>=0.15
datasets>=3.0
accelerate>=1.0
